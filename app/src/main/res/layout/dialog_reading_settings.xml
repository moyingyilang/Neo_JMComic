<?xml version="1.0" encoding="utf-8"?>
<!-- 
LinearLayout - 垂直线性布局，适合表单式对话框
padding: 内边距确保内容不贴边
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- 对话框标题 -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/reading_settings"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginBottom="16dp" />

    <!-- 阅读模式标题 -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/reading_mode"
        android:textSize="16sp"
        android:layout_marginBottom="8dp" />

    <!-- 
    RadioGroup - 单选按钮组，确保只能选择一个模式
    orientation: 水平排列两个选项
    -->
    <RadioGroup
        android:id="@+id/rgReadingMode"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <!-- 横向滚动选项 -->
        <RadioButton
            android:id="@+id/rbHorizontal"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/horizontal_mode" />

        <!-- 纵向滚动选项 -->
        <RadioButton
            android:id="@+id/rbVertical"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/vertical_mode" />

    </RadioGroup>

    <!-- 分割线 -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#E0E0E0"
        android:layout_marginVertical="16dp" />

    <!-- 亮度调节标题 -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/brightness"
        android:textSize="16sp"
        android:layout_marginBottom="8dp" />

    <!-- 
    SeekBar - 拖动条，用于调节亮度
    max: 最大值100，对应0-100%亮度
    progress: 默认50%亮度
    -->
    <SeekBar
        android:id="@+id/sbBrightness"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:max="100"
        android:progress="50" />

</LinearLayout>